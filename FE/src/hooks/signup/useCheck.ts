// const useCheck = () => {
//   const checkEmail = (
//     overlap?: string,
//     callback?: (str: string) => void,
//   ) => {
//     if (valid === '이메일형식이 올바르지 않습니다.') {
//       const str = '이메일형식이 올바르지 않습니다.';
//       callback(str);
//     }
//   };
//   const checkNick = (
//     overlap?: string,
//     valid?: string,
//     callback?: (str: string) => void,
//   ) => {};

//   return { checkEmail, checkNick };
// };

// export default useCheck;
